import style from './NoiseOverlay.module.css'

export default function NoiseOverlay() {
    return (
        <div className={`${style.noise}`} >
            {/* <img src="style/noise-grain.png"/> */}
        </div>
    );
}